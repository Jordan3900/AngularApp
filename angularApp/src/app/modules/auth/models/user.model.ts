// Why do you have the same model as the one in the users module?

export interface IUser {
    id: number
    email: string
    password: string
    firstName: string
    lastName: string
    userName: string
    fullName: string
}