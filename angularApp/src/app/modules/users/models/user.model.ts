// In TypeScript the interfaces are not prefixed with I
// And the properties are using the camel case convention 
// https://github.com/Microsoft/TypeScript/wiki/Coding-guidelines

export interface IUser {
    id: number
    email: string
    first_Name: string
    last_Name: string
    avatar: string
}