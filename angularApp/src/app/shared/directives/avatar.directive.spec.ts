import { AvatarDirective } from './avatar.directive';

describe('Directive: Avatar', () => {
  it('should create an instance', () => {
    const directive = new AvatarDirective();
    expect(directive).toBeTruthy();
  });
});
